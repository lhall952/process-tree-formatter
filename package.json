{
  "name": "process-tree-formatter",
  "version": "0.1.0",
  "description": "Normalizes messy, inconsistent process listings into a clean, deterministic process tree",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test test-dist/test"
  },
  "license": "MIT",
  "author": "lhall952",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
